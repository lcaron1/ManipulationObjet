using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Jeux
{
    enum classe { combattant, mage, pretre, voleur }
    enum Caractère { Good, neutral, Evil }
    enum Peuple { humain, nain, elfe, gnome, hobbit, troll, orc, nazgul, balrog }
    enum Effet { neutre, poison, antipoison }
    enum Etat { ok, empoisonné, statufié, endormi }
}
